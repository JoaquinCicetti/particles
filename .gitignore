# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# local verification screenshots
scripts/*.png

# Licensed Aeonik fonts: the .otf sources stay local (not redistributed), but
# the subset .woff2 faces the site actually loads ARE committed, so a build
# from Git ships the real typeface. Regenerate with `pnpm fonts`.
public/fonts/*.otf

# Claude Code scratch + agent worktrees
.claude/
